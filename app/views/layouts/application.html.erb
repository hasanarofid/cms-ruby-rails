<!DOCTYPE html>
<html>
  <head>
    <title>SKM Malang - Admin Sewa Kasur</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path %>

    <link rel="icon" icon="image/png" href="/icon.png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div class="logo">
        <div class="logo-circle">skm</div>
        <div class="brand-name">MALANG <span style="font-size: 0.8rem; display: block; text-transform: none;">sewa kasur murah</span></div>
      </div>
      <nav>
        <ul>
          <li><%= link_to "Dashboard", root_path %></li>
          <li><%= link_to "Data Kasur", kasurs_path %></li>
          <li><%= link_to "Data Penyewa", penyewas_path %></li>
          <li><%= link_to "Data Sewa", sewas_path %></li>
          <li><%= button_to "Keluar", logout_path, method: :delete, class: "btn", style: "background: var(--skm-blue); color: white; border: none; padding: 5px 15px; font-size: 0.9rem;" %></li>
        </ul>
      </nav>
    </header>

    <main>
      <% if notice %>
        <div style="padding: 1rem; background: #d4edda; color: #155724; border-radius: 4px; margin-bottom: 1rem;">
          <%= notice %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <div class="footer">
      &copy; <%= Time.now.year %> skm MALANG - Sewa Kasur Murah
    </div>
  </body>
</html>
